import handleChange from './handleChange';
const resetAll = (dispatch) => {
    dispatch({
        type: 'reset',
    });
};
export const resetSingleField = (field, dispatch) => {
    return handleChange(field, dispatch);
};
export default resetAll;
